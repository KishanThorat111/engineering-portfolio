# Lessons land here in Phase 6, once the owner confirms them (blueprint §4.5).
# Tracked now so the glob loader resolves cleanly. Do not place a README.md
# here: the loader pattern is **/*.md, so any Markdown file here is parsed as
# a lesson entry.
